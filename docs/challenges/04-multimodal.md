# Challenge 4 — Design a Multimodal NeXus Application Definition

## Goal
Design a **custom NeXus application definition** for a multimodal experiment where fluorescence and diffraction measurements are acquired simultaneously. Use this definition to convert the provided dataset into a NeXus data file. The final submission must include the application definition and the NeXus data file, uploaded to NOMAD.

## What you get
- A directory containing raw experimental data files  
- A master metadata file with scan parameters  
- A minimal ELN YAML file with partial metadata  
- A mapping template (in JSON format) showing how fields can be mapped to NeXus structure (incomplete and must be adapted)
- You can find an example multimodal NeXus application definition below:

[Download NXmultimodal.yaml](../downloads/NXmultimodal.yaml){:target="_blank" .md-button}

